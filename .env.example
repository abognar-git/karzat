# Personal parlament.hu Web API access code (api-reg2@parlament.hu registration).
# The code is personal and every request is logged by OGYH — never commit .env, never share it.
PARLAMENT_API_TOKEN=

# The published origin (no trailing slash), used by the site build for citations and the Atom feeds' links.
# Unset: citations resolve in the browser, feed links are relative to the feed file.
# KARZAT_SITE_URL=https://example.org/karzat
